import React from 'react';
import Commands from './Commands';

const NewChat = ({children}) => {
  return (
    <div className="mt-5 pt-5">
      {children}
    </div>
  )
};

export default NewChat;
